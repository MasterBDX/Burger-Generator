import React from 'react';


const drawerToggle = (props) => {
    return (
            <div onClick={props.toggleDrawerClick}>
                MENU
            </div>
            )
}

export default drawerToggle;